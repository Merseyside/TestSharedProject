enableFeaturePreview("GRADLE_METADATA") //very important shit (in another case kodein doesn't work )

include ':app', ':shared'

include ':library'
project(':library').projectDir = new File(settingsDir, '../mvvm-clean-arch-library/library')
